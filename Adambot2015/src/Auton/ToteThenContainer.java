package Auton;
	//Assumess that robot is placed in line with tote and container on the x axis
	//that the tote is placed first, and a positive x will move roller end first
public class ToteThenContainer {
	/*static Timer alex = new Timer();
	public static void update(){
		//Interior.Interior.toggleRollers(1);
		alex.start();
	if(alex.get()< .5)
		Drive.Drive.DriveSpeed(.2, 0, 0);
	if(alex.get()> .5 && alex.get()<1)
		Interior.Interior.clamp(false);
		Interior.Interior.moveArm(1);
	if(alex.get()> 1 && alex.get()<1.5)
		Interior.Interior.clamp(true);
		Interior.Interior.moveArm(-1);
	if(alex.get()> 1.5 && alex.get()<4.5)
		Drive.Drive.DriveSpeed(0, 0, .5);
	if(alex.get()> 4.5 && alex.get()<5.5)
		Drive.Drive.DriveSpeed(-.2, 0, 0);
	if(alex.get()> 5.5 && alex.get()<6)
		Exterior.Exterior.clamp(true);
		Exterior.Exterior.moveArm(1);
	if(alex.get()> 6 && alex.get()<7.5)
		Drive.Drive.DriveSpeed(0, .8, 0);
	
	}*/
}
